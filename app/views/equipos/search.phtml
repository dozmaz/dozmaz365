<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(array("equipos/index", "Go Back")); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(array("equipos/new", "Crear ")); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Buscar result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
        <tr>
            <th>EQUIPOS Of ID</th>
            <th>NOMBRE</th>
            <th>LOGO</th>
            <th></th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $equipo): ?>
            <tr>
                <td><?php echo $equipo->EQUIPOS_ID ?></td>
                <td><?php echo $equipo->NOMBRE ?></td>
                <td><img style="max-width: 100px;" class="img-responsive" src="data:image/jpeg;base64,<?php echo base64_encode($equipo->LOGO) ?>"/></td>
                <td><?php echo $this->tag->linkTo(array("equipos/edit/" . $equipo->EQUIPOS_ID, "Editar")); ?></td>
                <td><?php echo $this->tag->linkTo(array("equipos/delete/" . $equipo->EQUIPOS_ID, "Eliminar")); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("equipos/search", "Primero") ?></li>
                <li><?php echo $this->tag->linkTo("equipos/search?page=" . $page->before, "Anterior") ?></li>
                <li><?php echo $this->tag->linkTo("equipos/search?page=" . $page->next, "Siguiente") ?></li>
                <li><?php echo $this->tag->linkTo("equipos/search?page=" . $page->last, "Último") ?></li>
            </ul>
        </nav>
    </div>
</div>
